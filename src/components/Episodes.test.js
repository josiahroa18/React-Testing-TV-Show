import React from 'react';
import { render } from '@testing-library/react';
import Episodes from './Episodes';

// Test that the episodes component renders when data is not passed down by props

// test('Initial component render', () => {
//     const {} = render(
//         <Episodes episodes={[]}/>
//     )
// })